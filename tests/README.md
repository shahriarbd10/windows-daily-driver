# Tests

Cross-tool integration tests and shared test fixtures.

Keep unit tests inside each tool folder when possible, and use this folder for:

- integration tests spanning multiple tools
- shared test data
- end-to-end smoke checks
